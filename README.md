# A2A
Agent A (Planner): understands a natural-language prompt and breaks it into actionable subtasks.  
Agent B (Executor): runs those subtasks using tools (e.g., call a weather API, then summarize).  
Goal: end-to-end flow from user text → plan → tool calls → results → final answer.
